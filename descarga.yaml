- hosts: win1
  collections:
    - community.windows
    - ansible.builtin.git

  tasks:

    - name: Git checkout
      ansible.builtin.git:
        repo: 'https://github.com/jjgl93/server.git'
        dest: /tmp
        version: v1
#    - git:
#        repo: git://foosball.example.org/path/to/repo.git
#        dest: /srv/checkout
#        version: dev
